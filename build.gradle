wrapper {
    gradleVersion = '5.2.1'
}

apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'maven'

if (project.hasProperty("topobyte")) {
    apply from: 'file:///' + System.getProperty("user.home") \
        + '/.gradle/topobyte.gradle'
}

group = pGroup
version = pVersion

repositories {
    maven {
        url 'http://mvn.topobyte.de'
    }
    maven {
        url 'http://mvn.slimjars.com'
    }
    mavenCentral()
}

dependencies {
    compile 'de.topobyte:osm4j-xml:1.2.0'
    compile 'de.topobyte:osm4j-pbf:1.2.0'
    compile 'de.topobyte:osm4j-tbo:1.2.0'
    compile 'de.topobyte:osm4j-geometry:1.2.0'

    compile 'de.topobyte:adt-geo:0.1.0'
    compile 'de.topobyte:jts2awt:0.1.0'
    compile 'de.topobyte:mercator-image:0.1.0'

    compile 'org.wololo:jts2geojson:0.14.3'

    compile 'org.apache.commons:commons-compress:1.8'
    compile 'joda-time:joda-time:2.9.7'
    compile 'com.google.guava:guava:19.0'

    compile 'org.slf4j:slf4j-api:1.6.1'
    runtime 'org.slf4j:slf4j-log4j12:1.6.1'
    runtime 'log4j:log4j:1.2.16'
}

task sourcesJar(type: Jar, dependsOn: classes) {
    classifier = 'sources'
    from sourceSets.main.allSource
}

artifacts {
    archives sourcesJar
}
